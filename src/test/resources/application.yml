server:
  port: 8090

spring:
  h2.console.enabled: true

  thymeleaf:
    cache: false
    enabled: true
    prefix: classpath:/templates/
    suffix: .html

  application:
    name: Bootstrap Spring Boot